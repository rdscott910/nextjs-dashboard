import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
  description: 'Manage customers and view payment history.',
};

export default function Page() {
  return <p>Customers Page</p>;
}